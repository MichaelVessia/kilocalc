pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: nix develop -c npx tsc --noEmit

    - name: test
      run: nix develop -c bun test

    - name: lint
      run: nix develop -c npx oxlint

    - name: format
      run: nix develop -c npx oxfmt src
